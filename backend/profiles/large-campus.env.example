# Large Campus Profile
# Configuration for large broadcast campuses, multiple sites, HA-ready
# Suitable for: Large broadcasters, campus-wide deployments, > 50 nodes, multiple registries

APP_PORT=8080
# Database - use connection pooling, consider HA setup
DATABASE_URL=postgres://nmos_user:secure_password@db-primary.your-campus.local:5432/go_nmos?sslmode=require&pool_max_conns=20
JWT_SECRET=CHANGE-THIS-TO-A-VERY-STRONG-RANDOM-SECRET-MINIMUM-64-CHARACTERS-FOR-PRODUCTION
CORS_ORIGIN=https://nmos-control.your-campus.local
INIT_ADMIN_USER=admin
INIT_ADMIN_PASSWORD=CHANGE-THIS-TO-A-VERY-STRONG-PASSWORD-MIN-20-CHARACTERS
LOG_DIR=/var/log/go-nmos

# Rate limiting - higher for campus-wide use
RATE_LIMIT_RPM=1200

# MQTT - required for real-time events across campus
MQTT_ENABLED=true
MQTT_BROKER_URL=tcp://mqtt-cluster.your-campus.local:1883
MQTT_TOPIC_PREFIX=go-nmos/flows/events
MQTT_WS_PORT=9001

# Authentication - required
DISABLE_AUTH=false

# HTTPS - required for production campus deployment
HTTPS_ENABLED=true
HTTPS_PORT=8443
CERT_FILE=/etc/ssl/certs/go-nmos.crt
KEY_FILE=/etc/ssl/private/go-nmos.key

# SDN Controller - typically used in large campuses
SDN_CONTROLLER_URL=https://sdn-controller.your-campus.local:8443

# Alerting - multiple channels recommended
ALERT_WEBHOOK_URL=https://monitoring.your-campus.local/api/alerts
ALERT_SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Notes:
# - Multiple registries expected (per site/region)
# - Multi-site routing and cross-site visibility important
# - PTP domain validation critical (multiple domains possible)
# - Alerting essential for operations
# - Database HA/clustering recommended
# - MQTT broker HA recommended
# - Consider load balancing for API
# - Regular backups and disaster recovery planning
# - Suitable for 50+ nodes, multiple sites
# - May need multiple backend instances behind load balancer
