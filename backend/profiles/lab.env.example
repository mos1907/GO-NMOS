# Lab / Single Node Profile
# Minimal configuration for development, testing, or single-node deployments
# Suitable for: Development, testing, small labs, single encoder/decoder setups

APP_PORT=8080
DATABASE_URL=postgres://postgres:postgres@localhost:5432/go_nmos?sslmode=disable
JWT_SECRET=lab-secret-change-in-production
CORS_ORIGIN=http://localhost:4173
INIT_ADMIN_USER=admin
INIT_ADMIN_PASSWORD=admin
LOG_DIR=/tmp/go-nmos-logs

# Rate limiting - relaxed for lab use
RATE_LIMIT_RPM=1200

# MQTT - enabled for real-time events, but optional
MQTT_ENABLED=true
MQTT_BROKER_URL=tcp://localhost:1883
MQTT_TOPIC_PREFIX=go-nmos/flows/events
MQTT_WS_PORT=9001

# Authentication - can be disabled for lab convenience
DISABLE_AUTH=false

# HTTPS - typically disabled in lab
HTTPS_ENABLED=false
HTTPS_PORT=8443
CERT_FILE=/certs/server.crt
KEY_FILE=/certs/server.key

# SDN Controller - typically not used in lab
SDN_CONTROLLER_URL=

# Alerting - minimal, just logging
# ALERT_WEBHOOK_URL=
# ALERT_SLACK_WEBHOOK_URL=

# Notes:
# - Single registry expected (internal discovery)
# - No multi-site considerations
# - PTP domain validation optional
# - Suitable for < 10 nodes
