# Backend listens on 9090 (frontend expects 9090)
APP_PORT=9090

# DB: connect to PostgreSQL (e.g. VM/container)
DATABASE_URL=postgres://postgres:postgres@192.168.248.133:5432/go_nmos?sslmode=disable

JWT_SECRET=murat

# Frontend from local dev -> Vite default: 5173
CORS_ORIGIN=http://localhost:4173

INIT_ADMIN_USER=admin
INIT_ADMIN_PASSWORD=murat
LOG_DIR=/tmp/go-nmos-logs
RATE_LIMIT_RPM=600

# Set to false if you don't need MQTT
MQTT_ENABLED=true
MQTT_BROKER_URL=tcp://localhost:1883
MQTT_TOPIC_PREFIX=go-nmos/flows/events